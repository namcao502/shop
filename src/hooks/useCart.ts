// src/hooks/useCart.ts (stub -- will be replaced in Task 7)
"use client";
import { useState } from "react";
export function useCart() {
  return { items: [], addItem: () => {}, updateQty: () => {}, removeItem: () => {}, clear: () => {}, totalItems: 0, subtotal: 0 };
}
